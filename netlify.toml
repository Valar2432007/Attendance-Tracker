[build]
  command = "npm install --prefix \"Attendance Tracker\" && npm run build --prefix \"Attendance Tracker\""
  publish = "Attendance Tracker/dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
